import * as React from 'react'
import styled from '@emotion/styled'

const Headline = () => {
    return <HeadlineStyle></HeadlineStyle>
}

export default Headline

// TODO 見出しタグをasで渡せるようにする
const HeadlineStyle = styled.p``
